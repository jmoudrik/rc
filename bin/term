#!/bin/sh
# tohle byl muj default, ale protoze jsem nebyl schopnej rozbehnout ikony, tak switchuju na
# gnome terminal
#/usr/bin/urxvt -sb +si +sk -fg green -bg black  -font 'xft:Terminus (TTF)' "$@"
#/usr/bin/uxterm -sl 65535 +si +sk -fg green -bg black -font terminus-16 "$@"
#/usr/bin/uxterm +si +sk -fg green -bg black $@

gnome-terminal "$@"

